# RAG and MCP System Requirements for Hospital Supply Chain Platform

# Core dependencies (already in main requirements.txt)
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0
python-multipart>=0.0.6

# RAG System Dependencies
chromadb>=0.4.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# Vector database alternatives (optional)
# faiss-cpu>=1.7.4  # Alternative vector database
# weaviate-client>=3.21.0  # Another vector database option

# Embedding and NLP
sentence-transformers>=2.2.2  # For better embeddings if not using Gemini
transformers>=4.30.0  # For text processing

# Database storage for documents
# sqlite3 is built into Python, no installation needed

# Model Context Protocol dependencies
typing-extensions>=4.0.0
# dataclasses>=0.8  # Built-in for Python 3.7+, not needed for Python 3.10

# LLM integration (already configured)
google-generativeai>=0.3.0
python-dotenv>=1.0.0

# Utility libraries
aiofiles>=23.0.0  # For async file operations
asyncio-throttle>=1.0.2  # For rate limiting API calls

# Optional: Advanced text processing
# spacy>=3.6.0  # For advanced NLP features
# nltk>=3.8.1   # For text preprocessing

# Optional: Better caching
redis>=4.5.0  # If you want Redis-based caching instead of in-memory

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0  # For testing async HTTP clients

# Optional: Production monitoring
# prometheus-client>=0.17.0  # For metrics
# structlog>=23.0.0          # For better logging
